import { Link, useNavigate, useLocation } from 'react-router-dom';
import { useState } from 'react';
import { Search, ShoppingCart, Menu, X, Gamepad2, LifeBuoy } from 'lucide-react';
import { useCart } from '@/context/CartContext';
export function PublicHeader() { const { count } = useCart(); const navigate=useNavigate(); const location=useLocation(); const [search,setSearch]=useState(''); const [open,setOpen]=useState(false); const go=(e:React.FormEvent)=>{e.preventDefault();if(search.trim())navigate('/loja?q='+encodeURIComponent(search.trim()));setOpen(false)}; const nav=(to:string,label:string)=><Link to={to} onClick={()=>setOpen(false)} className={

'text-sm font-medium transition-colors '+(location.pathname===to?'text-neon-300':'text-ink-200 hover:text-white')}>{label}</Link>; return <header className="sticky top-0 z-40 glass-strong border-b border-white/10"><div className="mx-auto max-w-7xl px-4"><div className="flex h-16 items-center gap-4"><Link to="/" className="flex items-center gap-2 shrink-0"><div className="grid h-9 w-9 place-items-center rounded-xl bg-neon-500/15 border border-neon-500/30"><Gamepad2 className="h-5 w-5 text-neon-400" /></div><span className="font-display text-lg font-bold tracking-tight text-white hidden sm:block">Nexus<span className="text-neon-400">Store</span></span></Link><nav className="hidden md:flex items-center gap-6">{nav('/','Início')}{nav('/loja','Loja')}{nav('/categorias','Categorias')}{nav('/suporte','Suporte')}</nav><form onSubmit={go} className="flex-1 max-w-md hidden sm:block"><div className="relative"><Search className="absolute left-3 top-1/2 -translate-y-1/2 h-4 w-4 text-ink-400" /><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Buscar produtos..." className="input pl-10 py-2.5 text-sm" /></div></form><div className="flex items-center gap-1 ml-auto"><Link to="/suporte" aria-label="Suporte" className="grid h-10 w-10 place-items-center rounded-xl text-ink-200 hover:bg-white/5 hover:text-white"><LifeBuoy className="h-5 w-5" /></Link><Link to="/carrinho" className="relative grid h-10 w-10 place-items-center rounded-xl text-ink-200 hover:bg-white/5 hover:text-white"><ShoppingCart className="h-5 w-5" />{count>0&&<span className="absolute right-0 top-0 grid h-5 min-w-5 place-items-center rounded-full bg-neon-500 px-1 text-[10px] font-bold text-ink-950">{count}</span>}</Link><button onClick={()=>setOpen(!open)} className="grid h-10 w-10 place-items-center rounded-xl text-ink-200 hover:bg-white/5 md:hidden">{open?<X/>:<Menu/>}</button></div></div>{open&&<div className="flex flex-col gap-4 border-t border-white/10 py-4 md:hidden">{nav('/','Início')}{nav('/loja','Loja')}{nav('/categorias','Categorias')}{nav('/suporte','Suporte')}</div>}</div></header>; }
