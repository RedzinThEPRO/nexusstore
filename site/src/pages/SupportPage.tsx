import { useState } from 'react';
const initial={name:'',email:'',subject:'',description:''};
export function SupportPage(){const [form,setForm]=useState(initial);const [status,setStatus]=useState('');const [busy,setBusy]=useState(false);const change=(key:keyof typeof initial)=>(e:React.ChangeEvent<HTMLInputElement|HTMLTextAreaElement>)=>setForm({...form,[key]:e.target.value});const submit=async(e:React.FormEvent)=>{e.preventDefault();setStatus('');if(Object.values(form).some(v=>!v.trim()))return setStatus('Preencha todos os campos.');if(!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(form.email))return setStatus('Informe um e-mail válido.');setBusy(true);try{const base=import.meta.env.VITE_API_URL||'';const res=await fetch(base+'/api/support',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)});if(!res.ok)throw new Error();setForm(initial);setStatus('Solicitação enviada com sucesso! Nossa equipe analisará sua mensagem.');}catch{setStatus('Não foi possível enviar agora. Tente novamente em instantes.')}finally{setBusy(false)}};return <section className="mx-auto max-w-3xl px-4 py-16"><div className="mb-8"><p className="text-sm font-semibold uppercase tracking-widest text-neon-400">Atendimento</p><h1 className="mt-2 font-display text-4xl font-bold text-white">Suporte</h1><p className="mt-3 text-ink-300">Envie sua dúvida e nossa equipe retornará pelo e-mail informado.</p></div><form onSubmit={submit} className="space-y-5 rounded-2xl border border-white/10 bg-white/[.03] p-6"><label className="block text-sm text-ink-200">Nome<input required value={form.name} onChange={change('name')} className="input mt-2" /></label><label className="block text-sm text-ink-200">E-mail<input required type="email" value={form.email} onChange={change('email')} className="input mt-2" /></label><label className="block text-sm text-ink-200">Assunto<input required value={form.subject} onChange={change('subject')} className="input mt-2" /></label><label className="block text-sm text-ink-200">Descrição<textarea required rows={6} value={form.description} onChange={change('description')} className="input mt-2 resize-y" /></label>{status&&<p role="status" className="rounded-xl border border-neon-500/30 bg-neon-500/10 p-3 text-sm text-neon-200">{status}</p>}<button disabled={busy} className="btn-primary w-full" type="submit">{busy?'Enviando...':'Enviar solicitação'}</button></form></section>}
